[workspace]
channels = ["https://prefix.dev/conda-forge"]
platforms = ["win-64"]
preview = ["pixi-build"]

[dependencies]
python = ">3.10"
numpy = ">=2.4.2,<3"
xrayphysics = { path = "." }
matplotlib = ">=3.10.8,<4"

[package]
name = "xrayphysics"
version = "1.2.1"

[package.build]
backend = { name = "pixi-build-rattler-build", version = "*" }
